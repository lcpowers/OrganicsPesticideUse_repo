---
title: "Creating a summary table"
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#### 1. Load packages and data
```{r}
library(rgdal)
library(sf)
library(tidyverse)
library(rgdal)
setwd("~/Projects/Organics/Working/organics_repo/Rmarkdown/")
options(scipen = 9999)

input_df = readOGR("../R_output/spatial/final_organics_data/2017/all_organics_pest_2017.shp") %>% 
  st_as_sf() %>% 
  as.data.frame() %>% 
  dplyr::select(-geometry)
```

#### 2. Filter data.frame and keep only the 5 most common organic crops
```{r top5_crops}
# CDFA Organic Column

input_df$comm_dt = as.factor(input_df$comm_dt)

df_fltr = input_df %>% 
     group_by(genus,comm_dt) %>% 
     summarise(total_fields = n(),
               organic_fields = sum(cdf_rgn),
               conv_fields = n()-sum(cdf_rgn)) %>% 
  filter(organic_fields > 9 & conv_fields > 9) %>% 
  arrange(-organic_fields)

common_crops = as.character(df_fltr$agrclss)
top5_crops = as.character(head(df_fltr$comm_dt,5))

# crop1 = as.character(top_crops[1])
# crop2 = as.character(top_crops[2])
# crop3 = as.character(top_crops[3])
# crop4 = as.character(top_crops[4])
# crop5 = as.character(top_crops[5])
```

#### 4. Initialize empty matrices for organic and conventional crops
```{r initialize_matrices}

O_matrix = matrix(ncol = 9, nrow = 7)
colnames(O_matrix) = c("total_fields","avg_fldsz","soil","Ann_KgAI_perH","birds","bees", "mmls","aqsp","reps")
rownames(O_matrix) = c("overall","sharedcrops",top5_crops[1],top5_crops[2],top5_crops[3],top5_crops[4],top5_crops[5])

C_matrix = matrix(ncol = 9, nrow = 7)
colnames(C_matrix) = c("total_fields","avg_fldsz","soil","Ann_KgAI_perH","birds","bees", "mmls","aqsp","reps")
rownames(C_matrix) = c("overall","sharedcrops",top5_crops[1],top5_crops[2],top5_crops[3],top5_crops[4],top5_crops[5])

```

#### 5. Fill in Organic Matrix
```{r organic_matrix}

# For all ORGANIC fields
org_df = raw_df %>% 
  dplyr::filter(organic == 1)
rowi = 1

O_matrix[rowi,1] = nrow(org_df)
O_matrix[rowi,2] = mean(org_df$hectars) # Average Field Size
O_matrix[rowi,3] = mean(org_df$soil_q, na.rm = TRUE) # Average Storie Index value
O_matrix[rowi,4] = sum(org_df$KgPstAI)/sum(org_df$hectars) # Average Kg AI per acre
O_matrix[rowi,5] = sum(org_df$KgABrds)/sum(org_df$hectars) # Average kg AI toxic to birds per acre
O_matrix[rowi,6] = sum(org_df$KgAiBes)/sum(org_df$hectars) # Average kg AI toxic to bees per acre
O_matrix[rowi,7] = sum(org_df$KgAMmml)/sum(org_df$hectars) # Average kg AI toxic to mammals per acre
O_matrix[rowi,8] = sum(org_df$KgAAqSp)/sum(org_df$hectars) # Average kg AI toxic to aquatic species per acre
O_matrix[rowi,9] = sum(org_df$KgARptA)/sum(org_df$hectars) # Average kg AI toxic to reptiles per acre

# For crops that occur on at least 10 organic fields and 10 conventional fields

df = org_df %>% 
  filter(comm_dt %in% common_crops)
rowi = 2

O_matrix[rowi,1] = nrow(df)
O_matrix[rowi,2] = mean(df$hectars) # Average Field Size
O_matrix[rowi,3] = mean(df$soil_q, na.rm = TRUE) # Average Storie Index value
O_matrix[rowi,4] = sum(df$KgPstAI)/sum(df$hectars) # Average Kg AI per acre
O_matrix[rowi,5] = sum(df$KgABrds)/sum(df$hectars) # Average kg AI toxic to birds per acre
O_matrix[rowi,6] = sum(df$KgAiBes)/sum(df$hectars) # Average kg AI toxic to bees per acre
O_matrix[rowi,7] = sum(df$KgAMmml)/sum(df$hectars) # Average kg AI toxic to mammals per acre
O_matrix[rowi,8] = sum(df$KgAAqSp)/sum(df$hectars) # Average kg AI toxic to aquatic species per acre
O_matrix[rowi,9] = sum(df$KgARptA)/sum(df$hectars) # Average kg AI toxic to reptiles per acre

for(i in 1:length(top5_crops)){
  
  df = org_df %>% 
    filter(comm_dt == top5_crops[i])
  
  print(top5_crops[i])
  
  O_matrix[i+2,1] = nrow(df)
  O_matrix[i+2,2] = mean(df$hectars) # Average Field Size
  O_matrix[i+2,3] = mean(df$soil_q, na.rm = TRUE) # Average Storie Index value
  O_matrix[i+2,4] = sum(df$KgPstAI)/sum(df$hectars) # Average Kg AI per acre
  O_matrix[i+2,5] = sum(df$KgABrds)/sum(df$hectars) # Average kg AI toxic to birds per acre
  O_matrix[i+2,6] = sum(df$KgAiBes)/sum(df$hectars) # Average kg AI toxic to bees per acre
  O_matrix[i+2,7] = sum(df$KgAMmml)/sum(df$hectars) # Average kg AI toxic to mammals per acre
  O_matrix[i+2,8] = sum(df$KgAAqSp)/sum(df$hectars) # Average kg AI toxic to aquatic species per acre
  O_matrix[i+2,9] = sum(df$KgARptA)/sum(df$hectars) # Average kg AI toxic to reptiles per acre

}

O_df = as.data.frame(round(O_matrix,4))
O_df$rownms = row.names(O_matrix)
write_csv(O_df,"../R_output/CSV/organic_summary_table_a.csv")
```

#### 6. Fill in Conventional Matrix
```{r conventional}

# For all CONVENTIONAL fields
conv_df = raw_df %>% 
  dplyr::filter(organic == 0)

rowi = 1

C_matrix[rowi,1] = nrow(conv_df)
C_matrix[rowi,2] = mean(conv_df$hectars) # Average Field Size
C_matrix[rowi,3] = mean(conv_df$soil_q, na.rm = TRUE) # Average Storie Index value
C_matrix[rowi,4] = sum(conv_df$KgPstAI)/sum(conv_df$hectars) # Average Kg AI per acre
C_matrix[rowi,5] = sum(conv_df$KgABrds)/sum(conv_df$hectars) # Average kg AI toxic to birds per acre
C_matrix[rowi,6] = sum(conv_df$KgAiBes)/sum(conv_df$hectars) # Average kg AI toxic to bees per acre
C_matrix[rowi,7] = sum(conv_df$KgAMmml)/sum(conv_df$hectars) # Average kg AI toxic to mammals per acre
C_matrix[rowi,8] = sum(conv_df$KgAAqSp)/sum(conv_df$hectars) # Average kg AI toxic to aquatic species per acre
C_matrix[rowi,9] = sum(conv_df$KgARptA)/sum(conv_df$hectars) # Average kg AI toxic to reptiles per acre

# For crops that occur on at least 10 organic fields and 10 conventional fields

df = conv_df %>% 
  filter(comm_dt %in% common_crops)
rowi = 2

C_matrix[rowi,1] = nrow(df)
C_matrix[rowi,2] = mean(df$hectars) # Average Field Size
C_matrix[rowi,3] = mean(df$soil_q, na.rm = TRUE) # Average Storie Index value
C_matrix[rowi,4] = sum(df$KgPstAI)/sum(df$hectars) # Average Kg AI per acre
C_matrix[rowi,5] = sum(df$KgABrds)/sum(df$hectars) # Average kg AI toxic to birds per acre
C_matrix[rowi,6] = sum(df$KgAiBes)/sum(df$hectars) # Average kg AI toxic to bees per acre
C_matrix[rowi,7] = sum(df$KgAMmml)/sum(df$hectars) # Average kg AI toxic to mammals per acre
C_matrix[rowi,8] = sum(df$KgAAqSp)/sum(df$hectars) # Average kg AI toxic to aquatic species per acre
C_matrix[rowi,9] = sum(df$KgARptA)/sum(df$hectars) # Average kg AI toxic to reptiles per acre

for(i in 1:length(top5_crops)){
  
  df = conv_df %>% 
    filter(comm_dt == top5_crops[i])
  
  print(top5_crops[i])
  
  C_matrix[i+2,1] = nrow(df)
  C_matrix[i+2,2] = mean(df$hectars) # Average Field Size
  C_matrix[i+2,3] = mean(df$soil_q, na.rm = TRUE) # Average Storie Index value
  C_matrix[i+2,4] = sum(df$KgPstAI)/sum(df$hectars) # Average Kg AI per acre
  C_matrix[i+2,5] = sum(df$KgABrds)/sum(df$hectars) # Average kg AI toxic to birds per acre
  C_matrix[i+2,6] = sum(df$KgAiBes)/sum(df$hectars) # Average kg AI toxic to bees per acre
  C_matrix[i+2,7] = sum(df$KgAMmml)/sum(df$hectars) # Average kg AI toxic to mammals per acre
  C_matrix[i+2,8] = sum(df$KgAAqSp)/sum(df$hectars) # Average kg AI toxic to aquatic species per acre
  C_matrix[i+2,9] = sum(df$KgARptA)/sum(df$hectars) # Average kg AI toxic to reptiles per acre

  }

C_df = as.data.frame(round(C_matrix,4))
C_df$rownms = row.names(C_matrix)
write_csv(C_df,"../R_output/CSV/conventional_summary_table_a.csv")

```

